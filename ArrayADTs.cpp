#include <iostream>
using namespace std;
struct myArray
{
    int total_size;
    int used_size;
    int *ptr;
};

void createArray(struct myArray * а, int tSize, int uSize){
    // (*a).total_size = tSize
}

int main() {
    myArray marks;
    return 0;
}